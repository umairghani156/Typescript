export const connecton: Connection = {
    CONNECTION_STRING: "CONNECTION STRING", 
    DB: "POSTGRESQL",
    DB_NAME: "TEST"

};

export type Connection = {
    CONNECTION_STRING: string,
    DB: string,
    DB_NAME: string
}